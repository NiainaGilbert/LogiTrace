info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/login.php"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "rakotoarisonniaina1@gmail.com",
        "password": "gilbert"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
