info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/declaration/update.php"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
