info:
  name: read
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/declaration/read.php"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id":"2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
